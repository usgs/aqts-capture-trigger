service: aqts-capture-trigger

provider:
  name: aws
  region: 'us-west-2'
  stage: ${opt:stage, 'test'}
  runtime: python3.8
  memorySize: 128
  deploymentBucket:
    name: iow-cloud-applications
  stackTags:
    "wma:applicationId": "Aquarius TS Capture Trigger"
    "wma:contact": "Andrew Yan ayan@usgs.gov"
    "wma:environment": "test"
    taggingVersion: 0.0.1
    "wma:costCenter": TBD

custom:
  accountNumber: ${ssm:/iow/aws/accountNumber}

functions:
  aqtsCaptureTrigger:
    handler: trigger.handler.lambda_handler
    role: arn:aws:iam::${self:custom.accountNumber}:role/csr-Lambda-Role
    onError:
      Ref: deadLetterQueue
    events:
      - s3:
          bucket: S3BucketTriggering
          event: s3:ObjectCreated:Put
    environment:
      STATE_MACHINE_ARN: arn:aws:states:us-west-2:${self:custom.accountNumber}:stateMachine:aqts-capture-state-machine-${self:provider.stage}
      AWS_DEPLOYMENT_REGION: ${self:provider.region}

resources:
  Resources:
    deadLetterQueue:
      Type: AWS::SNS::Topic
      Properties:
        DisplayName: aqts-trigger-sns-dlq-${self:provider.stage}
        TopicName: aqts-trigger-sns-dlq-${self:provider.stage}
    S3BucketTriggering:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: iow-retriever-capture-${self:provider.stage}
        AccessControl: Private
        LifecycleConfiguration:
          Rules:
            - ExpirationInDays: 1
              Status: Enabled
    AqtsCaptureTriggerPermissionTriggeringS3:
      Type: 'AWS::Lambda::Permission'
      Properties:
        FunctionName:
          'Fn::GetAtt':
            - AqtsCaptureTriggerLambdaFunction
            - Arn
        Principal: 's3.amazonaws.com'
        Action: 'lambda:InvokeFunction'
        SourceAccount:
          Ref: AWS::AccountId
        SourceArn: arn:aws:s3:::iow-retriever-capture-${self:provider.stage}

plugins:
  - serverless-python-requirements

package:
  exclude:
    - node_modules/**
    - Dockerfile
    - .dockerignore
    - Jenkinsfile
    - package.json
    - package-lock.json
